Ось твоя шпаргалка для майбутніх проєктів. Цей метод — «золотий стандарт» для Vite у 2026 році: він забезпечує **найменший бандл**, **миттєву швидкість** та **повну офлайн-підтримку**.

---

## 🛠 Крок 1: Встановлення плагіна

Тобі потрібен лише один пакет для роботи самого механізму. Все інше плагін зробить сам.

```bash
npm install -D unplugin-icons
npm install -D @svgr/core @svgr/plugin-jsx

```

---

## ⚙️ Крок 2: Налаштування `vite.config.js`

Додай плагін у конфіг. Параметр `autoInstall: true` — це магія, яка сама завантажить іконки з npm, як тільки ти пропишеш їхній імпорт у коді.

```javascript
import Icons from 'unplugin-icons/vite'
import react from '@vitejs/plugin-react-swc'

export default {
  plugins: [
    react(),
    Icons({
      compiler: 'jsx',   // Кажемо плагіну видавати React-компоненти
      autoInstall: true, // Автоматично качає @iconify-json/ім'я_паку
    }),
  ],
}

```

---

## 📝 Крок 3: Налаштування TypeScript (якщо є)

Щоб VS Code не підкреслював імпорти червоним, додай цей рядок у свій файл `src/vite-env.d.ts` або `src/env.d.ts`:

```typescript
/// <reference types="unplugin-icons/types/react" />

```

---

## 🚀 Крок 4: Використання в коді

Більше не потрібно реєструвати колекції чи використовувати загальний компонент `<Icon />`. Просто імпортуй потрібну іконку.

**Шаблон імпорту:** `~icons/{prefix}/{icon_name}`

```tsx
import IconCart from '~icons/bi/cart' // bi — Bootstrap Icons
import IconUser from '~icons/solar/user-bold' // solar — Solar Icons

const Header = () => {
  return (
    <nav>
      {/* Використовуєш як звичайний React-компонент */}
      <IconCart style={{ fontSize: '24px', color: 'blue' }} />
      <IconUser className="user-icon" />
    </nav>
  )
}

```

---

## 💡 Чому цей метод кращий за інші?

| Характеристика | Метод `addCollection` (твій старий) | Метод `unplugin-icons` (новий) |
| --- | --- | --- |
| **Розмір бандла** | 🔴 Гігантський (усі іконки паку в JS) | 🟢 Мінімальний (тільки використані SVG) |
| **Зручність** | Треба створювати окремі файли setup | Просто імпортуєш і працюєш |
| **TypeScript** | Потребує ручного опису типів | Типізація працює "з коробки" |
| **Tree-shaking** | Відсутній | **Працює ідеально** |

---

## 🔍 Де шукати назви іконок?

1. Заходь на [icones.js.org](https://icones.js.org/).
2. Обирай іконку.
3. Дивись на назву колекції (наприклад, `mdi`) та назву іконки (наприклад, `home`).
4. Твій імпорт: `import IconHome from '~icons/mdi/home'`.

> [!IMPORTANT]
> При такому підході тобі **не потрібен** пакет `@iconify/react`. Можеш його сміливо видаляти (`npm uninstall @iconify/react`), щоб не засмічувати проєкт.

